
#include <stdio.h>
#include <unistd.h>
/**
* main - Entry point
* Description. To prints the alphabet in lowercase
* Return: Always Zero
*/
int main(void)
{


int ch;

for (ch = a; ch <= z; ch++)
{
putchar(ch);
}
putchar(n);
return (0);
}

