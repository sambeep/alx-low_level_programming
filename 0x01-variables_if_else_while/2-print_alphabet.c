
#include <stdio.h>
#include <unistd.h>
/**
* main - Entry point
* Description: prints the alphabet in lowercase
* Return: Always 0
*/
int main(void)
{
int ch;
char a;
char z;

for (ch = a; ch <= z; ch++)
{
putchar(ch);
}
putchar();
return (0);
}

